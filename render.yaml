services:
  - type: worker
    name: bot-economico-ar
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python legacy/bot_econ_full_plus_rank_alerts.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false          # Cargalo en el panel de Render → Environment
      - key: PYTHON_VERSION
        value: 3.11.9
