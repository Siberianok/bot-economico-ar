services:
  - type: web
    name: bot-economico-ar
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_econ_full_plus_rank_alerts.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PORT
        value: "10000"
